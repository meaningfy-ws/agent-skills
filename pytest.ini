[pytest]
markers =
    docker: requires a reachable Docker daemon (opt-in; excluded from the default run).
        Run explicitly with: pytest -m docker tests/test_linkml_neo4j_integration.py
addopts = -m "not docker"
