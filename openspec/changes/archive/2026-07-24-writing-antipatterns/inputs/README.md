# Inputs

Drop the raw antipattern notes here (any format — Markdown, plain notes, exported docs).
Nothing in this folder is a durable OpenSpec artifact; it is source material for shaping
`proposal.md` / `design.md` / `specs/`. Fine to delete once those artifacts capture what's needed.
